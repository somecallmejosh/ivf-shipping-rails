<!DOCTYPE html>
<html>
  <head>
    <title>IvfCryoShipping</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="relative flex flex-col min-h-screen overflow-hidden text-ivf-blue">
      <div class="relative z-10 flex items-center h-24 px-4 bg-white shadow-md">
        <header class="flex items-center justify-between w-full h-full mx-auto max-w-screen-2xl">
          <a href="/" aria-label="Home page" class="w-28 lg:w-32">
            <%= render '/shared/svgs/logo.svg' %>
          </a>
          <nav class="flex items-center h-full">
            <ul class="flex items-center h-full space-x-6 font-medium">
              <% if current_user %>
                <li><%= link_to 'Logout', destroy_user_session_path, method: :delete  %></li>
              <% else %>
                <li><%= link_to 'Login', new_user_session_path  %></li>
                <li><a href="/users/sign_up">Register</a></li>
              <% end %>
            </ul>
          </nav>
        </header>
      </div>
      <% if notice %>
        <div class="relative z-20 px-4 py-4 bg-green-100 lg:px-12 ">
          <div class="flex items-center mx-auto space-y-x max-w-screen-2xl">
            <%= heroicon "shield-check", options: { class: "w-4 h-4" } %>
            <p><%= notice %></p>
          </div>
        </div>
      <% end %>
      <% if alert %>
        <div class="relative z-20 px-4 py-4 bg-red-200 lg:px-12 ">
          <div class="flex items-center mx-auto space-x-2 max-w-screen-2xl">
            <%= heroicon "shield-exclamation", options: { class: "w-4 h-4" } %>
            <p><%= alert %></p>
          </div>
        </div>
      <% end %>
      <div class="relative z-10 flex flex-col flex-grow">
        <%= yield %>
      </div>
      <footer class="relative z-10 bg-white border-t border-ivf-blue-25">
        <div class="flex items-center justify-between w-full h-full px-4 py-8 mx-auto max-w-screen-2xl">
          <a href="/" aria-label="Home page" class="w-28">
            <%= render '/shared/svgs/logo.svg' %>
          </a>
          <div>
            <p>©<%= Date.today.year %>. IVFCRYO, LLC. All rights reserved.</p>
          </div>
        </div>
      </footer>
      <svg role="presentation" aria-hidden="true" viewBox="0 0 87.15 87.26" class="absolute top-0 left-0 z-0 w-screen text-gray-100 transform -translate-x-1/2 opacity-50 fill-current template-texture"><path d="M43.53,0A43.63,43.63,0,0,0,0,40.84H13.7A29.93,29.93,0,1,1,43.5,73.59h0A29.88,29.88,0,0,1,13.93,48H.11A43.63,43.63,0,1,0,43.53,0Z"></path> <path d="M67.7,43.62a24.08,24.08,0,0,0-48-2.78h26L52.82,48H19.93A24.07,24.07,0,0,0,67.7,43.62Z"></path></svg>
    </div>
  </body>
</html>
